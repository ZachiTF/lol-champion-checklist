// Optional local-only config file.
// Copy this file to config.local.js and keep it untracked.
// Note: This key is still visible in browser devtools/network if used from frontend code.
window.APP_CONFIG = {
  RIOT_API_KEY: "",
};
